# Local exports from Google Sheets — not part of the source repo
*.csv

# Financial report PDFs — uploaded to Google Drive, not stored in git
*.pdf

# Apps Script — contains API keys, deployed separately via script.google.com
Code.gs
